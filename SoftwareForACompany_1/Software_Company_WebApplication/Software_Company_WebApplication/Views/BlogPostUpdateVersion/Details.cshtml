@using System.Text;
@model Software_Company_WebApplication.Models.Blog_tbl

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Blog_tbl</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Title)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Title)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Date)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Date)
        </dd>
        
        <dt>
            <br />
            @Html.DisplayNameFor(model => model.BlogImage)
        </dt>

        <dd>
            <br />
            @*@Html.DisplayFor(model => model.BlogImage)*@
            @{
                var imgSrc = "";
                if (Model.BlogImage != null)
                {
                    var base64 = Convert.ToBase64String(Model.BlogImage);
                    imgSrc = String.Format("data:image/gif;base64,{0}", base64);
                }
            }
            <img src="@imgSrc" alt="This person did not post with an image" style="border-radius: 10px;" />
            <br /><br />
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Text)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Text)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.Id }) |
    @Html.ActionLink("Back to List", "Index")
</p>
